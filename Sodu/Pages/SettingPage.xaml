<Page
    x:Class="Sodu.Pages.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:local="using:Sodu.Pages"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    DataContext="{Binding Instance.SettingPageViewModelInstance,Source={ StaticResource  Locator}}"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
   >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="TextTitle"   Text="设置" TextTrimming="WordEllipsis" Grid.Column="1"  HorizontalAlignment="Left"  VerticalAlignment="Center" Margin="55,0,0,0"  FontSize="18"></TextBlock>

        <ScrollViewer  Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,5,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="1" Grid.Row="1"  Text="自动登录(保存用户信息，直接登陆)" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ToggleSwitch x:Name="togAutoLogin" Grid.Column="1" Grid.Row="2" Foreground="Black" IsOn="{Binding IfAutoLogin,Mode=TwoWay}"  >
                    <!--<Interactivity:Interaction.Behaviors >
                        <Core:EventTriggerBehavior   EventName="Toggled">
                            <Core:InvokeCommandAction Command="{Binding    SaveCommand}"  CommandParameter="0"/>
                        </Core:EventTriggerBehavior>
                    </Interactivity:Interaction.Behaviors>-->
                </ToggleSwitch>

                <TextBlock Grid.Column="1" Grid.Row="3"  Text="登陆后，自动添加点击小说到个人收藏" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ToggleSwitch x:Name="togAutoAdd" Grid.Column="1"  Grid.Row="4" Foreground="Black"   IsOn="{Binding IfAutAddToShelf,Mode=TwoWay}">
                    <!--<Interactivity:Interaction.Behaviors >
                        <Core:EventTriggerBehavior   EventName="Toggled">
                            <Core:InvokeCommandAction Command="{Binding    SaveCommand}" />
                        </Core:EventTriggerBehavior>
                    </Interactivity:Interaction.Behaviors>-->
                </ToggleSwitch>

                <TextBlock Grid.Column="1" Grid.Row="5"  Text="是否在2G/3G/4G下缓存小说" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ToggleSwitch x:Name="togDwonloadBook" Grid.Column="1"  Grid.Row="6" Foreground="Black"   IsOn="{Binding IfDownloadInWAAN,Mode=TwoWay}">
                    <!--<Interactivity:Interaction.Behaviors >
                        <Core:EventTriggerBehavior   EventName="Toggled">
                            <Core:InvokeCommandAction Command="{Binding    SaveCommand}" />
                        </Core:EventTriggerBehavior>
                    </Interactivity:Interaction.Behaviors>-->
                </ToggleSwitch>


                <TextBlock Grid.Column="1" Grid.Row="7"  Text="正文字体大小" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ComboBox x:Name="font" Grid.Column="1" Grid.Row="8" Foreground="Black" ItemsSource="{Binding FontSzieList}"  VerticalAlignment="Center" SelectedItem="{Binding  TextFontSzie,Mode=TwoWay}"  FontSize="18">
                    <!--<Interactivity:Interaction.Behaviors >
                        <Core:EventTriggerBehavior   EventName="SelectionChanged">
                            <Core:InvokeCommandAction Command="{Binding    SaveCommand}" />
                        </Core:EventTriggerBehavior>
                    </Interactivity:Interaction.Behaviors>-->
                </ComboBox>

                <TextBlock Grid.Column="1" Grid.Row="9"  Text="是否开启夜间模式" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ToggleSwitch  Grid.Column="1"  Grid.Row="10" Foreground="Black"   IsOn="{Binding IsNightModel,Mode=TwoWay}"/>

                <TextBlock Grid.Column="1" Grid.Row="11"  Text="是否开启横屏模式" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ToggleSwitch  Grid.Column="1"  Grid.Row="12" Foreground="Black"   IsOn="{Binding IsLandscape,Mode=TwoWay}"/>

                <TextBlock Grid.Column="1" Grid.Row="13"  Text="是否预读下一章" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="18"></TextBlock>
                <ToggleSwitch  Grid.Column="1"  Grid.Row="14" Foreground="Black"   IsOn="{Binding IsPreLoad,Mode=TwoWay}"/>

                <Button Style="{StaticResource  ContentButtonStyle}" Grid.Row="15" Grid.Column="1" Click="Button_Click" Padding="0" HorizontalAlignment="Left" Width="200" >
                    <TextBlock   Text="QQ交流群：568856882" VerticalAlignment="Center" FontSize="18"  Foreground="Black"></TextBlock>
                </Button>


            </Grid>
        </ScrollViewer>



        <!--<CommandBar  Grid.Row="2"   ClosedDisplayMode="Compact"  IsOpen="False" >
            <AppBarButton Icon="Save" Label="保存"  Command="{Binding SaveCommand}"/>
        </CommandBar>-->
    </Grid>

</Page>
